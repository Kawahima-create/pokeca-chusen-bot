# ローカル実行用。実際の値を入れて `.env` にコピーし、`source .env` してから実行。
# GitHub Actions では Settings > Secrets に同名で登録する（このファイルは使わない）。

export DISCORD_BOT_TOKEN="ここにBotトークン"
export DISCORD_CHANNEL_ID="通知先チャンネルのID（18桁前後の数字）"
